---
const { name, price, image } = Astro.props;
---
<div class="border p-4 rounded-lg shadow-md hover:shadow-lg transition">
  <img src={image} alt={name} class="w-full h-48 object-cover mb-2 rounded" />
  <h2 class="text-lg font-semibold">{name}</h2>
  <p class="text-blue-600 font-bold">৳{price}</p>
</div>
